<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

	<entry key="loginMember">
		SELECT 
			   MEM_NO
			 , MEM_ID
			 , MEM_PWD
			 , MEM_NAME
			 , PHONE
			 , EMAIL
			 , MEM_BIRTH
			 , ENROLL_DATE
			 , MODIFY_DATE
			 , STATUS
		  FROM 
		  	   MEMBER
		 WHERE 
		       STATUS = 'Y'
		    OR STATUS = 'A'
		   AND MEM_ID = ?
		   AND MEM_PWD = ?
	</entry>
	
	
	<!-- [휘경] 관리자 페이지에서 관리자로 로그인 -->
	<entry key="AdminLogin">
		SELECT 
		       MEM_NO
		     , MEM_ID
		     , MEM_PWD
		     , MEM_NAME
		     , PHONE
		     , EMAIL
		     , MEM_BIRTH
		     , ENROLL_DATE
		     , MODIFY_DATE
		     , STATUS
		  FROM
		       MEMBER
		 WHERE 
		       STATUS = 'A'
		   AND MEM_ID = ?
		   AND MEM_PWD = ?		
	</entry>
	
	<!-- 민국(회원가입) -->
	<entry key="insertMember">
		INSERT
		  INTO MEMBER
		       (
		         MEM_NO
		       , MEM_ID
		       , MEM_PWD
		       , MEM_NAME
		       , PHONE
		       , EMAIL
		       , MEM_BIRTH
		       )
		 VALUES
		       (
		         SEQ_MNO.NEXTVAL
		       , ?
		       , ?
		       , ?
		       , ?
		       , ?
		       , ?
		       )
	</entry>
	

	
	<!-- [휘경] 멤버 수 조회(활동회원, 관리자) -->
	<entry key="selectMemberCount">
		SELECT COUNT(*) MEMBERCOUNT
		  FROM MEMBER
		 WHERE STATUS IN ('A', 'Y')
	</entry>
	
	<!-- [휘경] 해당 페이지의 멤버 리스트 조회(활동회원, 관리자) -->
	<entry key="selectList">
		SELECT *
		  FROM (
		        SELECT
		               ROWNUM RNUM
		             , A.*
		          FROM (
		                SELECT
		                       MEM_NO
		                     , MEM_ID
		                     , MEM_NAME
		                     , EMAIL
		                     , PHONE
                     		 , ENROLL_DATE
		                  FROM MEMBER
		                 WHERE STATUS IN ('A', 'Y')
		                 ORDER
		                    BY ENROLL_DATE DESC
		               )A  
		       )
		  WHERE RNUM BETWEEN ? AND ?
	</entry>
	
	
	<!-- [휘경] 회원 상세조회 -->
	<entry key="selectMember">
		SELECT
		       MEM_NO
		     , MEM_ID
		     , MEM_NAME
		     , PHONE
		     , EMAIL
		     , MEM_BIRTH
		  FROM MEMBER
		 WHERE STATUS IN ('A', 'Y', 'B')
		   AND MEM_NO = ?
	</entry>
	
	
	<!-- [휘경] 관리자 권한으로 비밀번호 초기화 -->
	<entry key="resetPwd">
		UPDATE
			MEMBER
		       SET MEM_PWD = MEM_ID	  
		  WHERE MEM_NO = ?
	</entry>
	
	
	<entry key="insertMember">
		INSERT
		  INTO MEMBER
		       (
		         USER_NO
		       , USER_ID
		       , USER_PWD
		       , USER_NAME
		       , PHONE
		       , EMAIL
		       , ADDRESS
		       , INTEREST
		       )
		 VALUES
		       (
		         SEQ_UNO.NEXTVAL
		       , ?
		       , ?
		       , ?
		       , ?
		       , ?
		       , ?
		       , ?
		       )
	</entry>
	
	<entry key="idCheck">
		SELECT 
		       COUNT(*) "COUNT"
		  FROM MEMBER
		 WHERE USER_ID = ?
	</entry>

<entry key="idCheck">SELECTCOUNT(*) "COUNT"FROM MEMBERWHERE USER_ID = ? </entry>
</properties>



